{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "array",
  "description": "A list of usernames for a specific category",
  "items": {
    "type": "string",
    "pattern": "^[a-zA-Z0-9_]{1,15}$",
    "description": "A valid username (1-15 alphanumeric characters or underscore)"
  },
  "uniqueItems": true
}
